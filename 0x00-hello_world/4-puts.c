#include <stdio.h>
#include <string.h>

/**
 * main - prints an output
 *
 * Return: 0
 */

int main(void)
{
	char str1[49];

	strcpy(str1, "\"Programming is like building a multilingual puzzle");
	puts(str1);

	return (0);
}
